// The 'DOMContentLoaded' event is used to run code
// only once the DOM has finished loading. In other words,
// when all of the HTML on the page is ready to be manipulated.
document.addEventListener('DOMContentLoaded', function () {
  $('.chosen-select').chosen({width: '500px'})
});
